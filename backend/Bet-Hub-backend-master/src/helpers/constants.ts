export enum ERROR_NAMES {
  UNAUTHORIZE = 'UnauthorizedError',
  FORBIDDEN = 'ForbiddenError',
}

export enum USER_STATUSES {
  ACTIVE = 'active',
  DEACTIVATED = 'deactive',
  DELETED = 'deleted',
}
