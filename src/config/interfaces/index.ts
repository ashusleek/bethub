export interface News {
  _id: string;
  title: string;
  description: string;
}

export interface ICompany {
  _id: string;
  name: string;
  investment: number;
}

export interface ISuggestion {
  _id: string;
  title: string;
  description: string;
}
